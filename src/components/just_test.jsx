
// eslint-disable-next-line react/prop-types
const Just_Test = (props) => {

  return (
    <div>
      <p>আপনার নাম: {props.name} boish: {props.age}</p>
    </div>
  );
};

export default Just_Test;